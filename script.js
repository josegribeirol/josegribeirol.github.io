const pageBody = document.querySelector(".page-body");

const divDescription = document.createElement('div');

divDescription.textContent = 'Data Science & Web Development';

pageBody.appendChild(divDescription);